# Compose-level environment variables
# Copy to deploy/.env and customize per project. Never commit deploy/.env

POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=app_db

# Host ports (customize if conflicts)
API_PORT=3001
WEB_PORT=3000

# Image tags — must match PROJECT/AGENTS.MD
PROJECT_SLUG=todo-app
